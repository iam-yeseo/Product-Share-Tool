{
  "name": "product-share-tool",
  "private": true,
  "scripts": {
    "build": "node scripts/build-pages.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check.cjs",
    "start": "python3 -m http.server 8942 --bind 127.0.0.1"
  }
}
