# 검증 기록

2026-09-07, `feat/automation-editor` 구현 기준.

## 자동 검사

- `npm run build`, `npm test` 9개, `npm run check`, `git diff --check` 통과.
- 몰별 코드 차이와 앞자리 0, 단일/다중 이미지 파일명, HTML 순서, 수동 파일명과 잘못된 주소, 검사 무효화, 4차 카테고리/폴더 상속, 이미지 디코딩 실패/시간 초과, 완료 상태 제외 저장, 독립적인 행 복사, 지연된 이전 목록 응답 무시를 검사.

## 브라우저

- 루트에서 edit 이동, 기본/자동화 편집 전환, view의 읽기 전용 상품 필드와 완료 체크.
- 소매/도매 `009006` 각각 Canon/BOLIN으로 표시·저장.
- JPG/PNG 확장자 변경에 따른 URL 변경, 다중 이미지 순서 변경과 HTML 일치.
- 실제 외부 이미지 `system/avmatrix-sd1242-4k-1.jpg` 및 `-2.jpg`: 각각 950×5346, 950×3010으로 정상 판정.
- 존재하지 않는 이미지 주소: 로딩 실패. 주소 편집 후 이전 정상 판정 무효화.
- 분리된 QA 데이터에서 3차/4차 카테고리, 기본 폴더 연결, 폴더 추가·저장·새로고침, 소매/도매 독립성 확인.
- 390px 모바일에서 설정/자동화 상세 편집의 가로 넘침 수정 및 확인.

## 운영 Supabase

사용자가 승인한 범위로 마이그레이션을 적용했고 보안 advisor 경고는 0건이었다.

별도 테스트 목록에서 실제 anon 저장 함수, 썸네일 업로드와 공개 이미지 로딩, 저장 후 새로고침, DOM JSON의 ready/saved 상태, 코드/HTML/검사 결과 재조회를 확인했다. view에서 완료 체크 후 edit에서 다시 저장해도 완료 시각이 유지됐다. 잘못된 상품 값을 보낸 저장은 목록 수정까지 트랜잭션 전체가 롤백됨을 anon 역할로 확인했다.

테스트 상품과 목록은 검증 후 제거했다. 덮어쓰기/삭제 권한을 추가하지 않는 정책에 따라 68바이트 PNG 검증 원본 1개는 Storage에 남아 있다. 기존 상품 정보는 수정하지 않았다.

카테고리 초기값은 소매 144개, 도매 135개, 이미지 폴더 21개. 신규 Storage 정책은 지정 버킷·기존 목록 경로의 INSERT만 허용하며 파일 형식/6MB 제한을 확인했다.

## 적용 범위

기존 GitHub Pages는 main 브랜치를 배포한다. 이 기능 브랜치의 프론트엔드는 PR 병합 전까지 운영 도메인에 배포되지 않는다. 운영 DB의 추가 필드와 함수는 기존 프론트엔드와 호환된다.

## 브랜드 선택 방식 (2026-09-07, `claude/brand-selection-mode-l5m1em`)

- `npm run build`, `npm test` 14개, `npm run check`, `git diff --check` 통과. 브랜드 매칭(대소문자·공백·기호 무시), 중복·형식 검증, 이전 설정 형식 호환, 내보내기의 `brandCode`/`brandRegistered`, 임시 UI 플래그 제외, `image_usage` 제거, 엑셀 브랜드 매칭 통계를 추가로 검사.
- 분리된 QA 미리보기(Chromium): 열 19개이며 '이미지 사용 여부' 머리글 없음. 기본 편집에서 이미지 열 너비 0, 자동화 편집으로 바꾸면 100px로 복귀. 브랜드 선택 → 직접 입력 전환 시 입력칸에 자동 포커스, **목록** 버튼으로 복귀 시 일치하는 이름은 선택되고 없는 이름은 비워짐. 행 추가 시 브랜드 선택에 포커스.
- 불러오기 확인 창에 "자동 선택 2개 · 직접 입력 1개 · 비어 있음 1개" 표시. 적용 후 `tilta`→TILTA 선택, `Electro Voice`→Electro-Voice 선택, 미등록 이름은 직접 입력칸으로 표시. 저장 후 DB 문자열과 DOM JSON의 `brandCode`(165/189)·`brandRegistered` 확인. SheetJS CDN이 차단된 환경이라 파일 파싱 자체는 단위 검사 범위 밖이며, 파싱 결과를 앱의 확인 창 경로로 넣어 검증.
- 보기 페이지: 미등록 브랜드에 **직접 입력** 표시, 이미지 열 유지.
- 설정 > 브랜드: 84개 표시, 검색, `tilta` 중복 추가 거부, 추가·이름 수정·사용 중지·삭제 후 저장. 사용 중지한 브랜드는 편집 선택지에서 숨겨지고, 이름이 바뀐 브랜드를 쓰던 상품은 직접 입력으로 표시됨. 390px에서 가로 넘침 없음.
- 운영 Supabase에는 적용하지 않았다. 브랜드 초기값 마이그레이션(`20260907150000_brand_settings.sql`)은 기존 설정에 `brands`가 없을 때만 덧붙인다.
- 후속: 운영 Supabase(`product-registration-tool`)의 `product_automation_v1` 행에 `brands`가 없어 배포 화면의 브랜드 목록이 비어 있었다. 브랜드 초기값 마이그레이션을 적용해 84개가 들어갔고(카테고리 144/135·폴더 21 유지), 아울러 `brands` 키가 없는 설정을 읽으면 `js/brand-defaults.js`의 같은 목록을 대신 쓰도록 폴백을 추가했다. QA 미리보기에서 `brands`를 지운 설정으로도 편집 페이지에 84개가 보이고 소문자 `avmatrix`가 AVMATRIX로 선택되며, 설정 저장 후 DB에 목록이 들어가는 것을 확인했다. 단위 검사 15개.

## UI/UX 2차 수정 (2026-09-10, `claude/magical-wozniak-bz3nxq`)

Figma `CALLTO` 파일의 Foundations(`30:113`) · Button(`30:115`) · Form(`30:116`) · Data Display(`30:117`) · Navigation(`30:118`)과 Design 페이지의 Main(`40:5`) · modal-default(`40:460`) · modal-img(`40:549`) · modal-brand(`40:637`)을 기준으로 확인했다.

### 자동 검사

- `npm run build`, `npm test` 37개, `npm run check`, `git diff --check` 통과.
- 추가한 단위 검사: 상품명 HTML 원문 보존과 표시용 텍스트 분리(엔티티·스크립트·이벤트 속성 포함), 이미지 주소 생성의 기본 설정값/직접 입력 전환과 기존 데이터 호환, Figma 모달 배치 순서, 소비자몰 정가 직접 입력, 남는 폭 배분 가중치.

### 브라우저 (Chromium, 분리된 QA 미리보기)

1656 · 1280 · 768 · 390px에서 확인했으며 운영 데이터는 사용하지 않았다.

- 표: `등록 필요` 3열 · `가격` 4열 2행 그룹 헤더, 좌우 끝까지 스크롤해도 체크·No.·브랜드·상품명 헤더와 본문의 고정 위치가 일치, 열 숨김·재표시 후에도 동일, 페이지 자체는 가로로 밀리지 않음.
- 768 · 390px에서 상품명만 고정되고 읽기 순서도 상품명이 먼저이며, 체크·브랜드·가격·편집 등 모든 열에 접근 가능. 고정 상품명은 가용 폭의 절반을 넘지 않음.
- `필요` 배지 `#DCFCE7`/`#15803D`, 완료 행과 완료 고정 셀 모두 `#F9FAFB`, 체크박스 선택으로 완료값이 바뀌지 않음.
- 참고링크·편집 열 너비 동일, 행의 일반 영역 클릭은 모달을 열고 복사·체크·링크·텍스트 드래그는 각 동작만 실행.
- 사이드바 `완료/전체`: 빈 리스트 `0/0`, 일부 완료 `3/12`, 완료 해제·재처리, 상품 추가·삭제 후 즉시 갱신.
- 상품 모달: Figma의 전체 폭 / 3열 / 2열 배치와 `카테고리 및 금액` 3열 × 2행, 소비자몰 정가 기본 미입력 + 직접 입력 전환, 스마트스토어 가격 입력 없음(판매가 연동), 스마트스토어 상품명의 `/` 제거·공백 포함 글자 수·50자 초과 경고·자동 절단 없음.
- 브랜드 모달: `ROXTONE`에 한글 검색명 `록스톤`을 넣고 `록스톤`·`ㄹㅅㅌ`·`roxtone`으로 검색, 결과 없음 표시, 확인 버튼 문구 `브랜드 선택`.
- 카테고리 모달: 소비자몰/도매몰 독립 선택, `009006`이 몰별로 다른 경로로 표시, 저장 코드 앞자리 0 보존, 하위 모달 취소가 하위 선택만 취소하고 부모 입력·상태 유지.
- 이미지 탭: 썸네일 160×160, 섹션 순서(썸네일 → 주소 만들기 → 이미지 목록 → HTML), 생성용 브랜드·모델명이 기본 설정값을 따르고 직접 입력으로만 변경, 자동 생성 2장·순서 이동·HTML 순서 일치, 미리보기 공간 예약, `자동화에 필요한 정보가 준비되었습니다` 요약 제거.
- 흐름: 목록 열기 → 가로 스크롤 → 복사 → 편집 → 브랜드·카테고리 선택 → 이미지 탭 → 취소(목록 미반영·저장 비활성) → 재편집 → 목록 반영 → 상단 저장 → 새로고침까지 값과 HTML 원문·완료값 유지.
- GNB: `?tab=brands` / `?tab=categories` 직접 접근·새로고침·내부 탭 전환에서 현재 위치와 `aria-current`가 유지되고, 카테고리 화면 진입 중 브랜드 카드가 나타나지 않음.
- 모션: `prefers-reduced-motion: reduce`에서 모달이 전환 없이 즉시 닫힘.
- 검수용 HTML 상품명(`<font …>`, `<img … onerror>`, `<script>`)이 목록에서 텍스트로만 보이고 실행되지 않음.

### 남은 제한

- 이 샌드박스는 외부 CDN(`cdn.jsdelivr.net`) 접근이 차단되어 Pretendard 웹폰트 실제 로딩은 브라우저에서 확인하지 못했다. 링크는 Pretendard 공식 배포 경로를 사용했고, 실패 시 시스템 한글 글꼴로 대체된다.
- 운영 Supabase에는 적용하지 않았다. 스키마 변경은 없으며 `automation.generator`에 `brandManual`/`productManual` 플래그만 덧붙는다. (기존 값이 있으면 직접 입력으로 승계)

## UI/UX 2차 수정 · 후속 반영 (2026-09-10, `claude/magical-wozniak-bz3nxq`)

사용자 확인 후 요청한 8개 항목을 반영하고 다시 검수했다.

### 자동 검사

- `npm run build`, `npm test` 38개, `npm run check`, `git diff --check` 통과.
- 추가한 단위 검사: 스마트스토어 상품명의 `/` → 공백 한 칸 규칙, 닫는 태그(`</font>`)가 규칙에 깨지지 않도록 읽기용 텍스트에서 생성, 직접 입력 중에는 `/`만 바꾸고 공백은 건드리지 않음.

### 브라우저 (Chromium, 분리된 QA 미리보기)

anime.js CDN이 막힌 환경이라 **라이브러리가 있을 때와 없을 때 두 경우 모두** 확인했다.

- 상품명 셀의 복사 버튼 2개: 첫 번째는 HTML 원문, 두 번째는 스마트스토어 상품명(저장된 값이 있으면 그 값)을 복사한다.
- 표 머리글의 `체크` 텍스트를 없애고 전체 선택 체크박스만 남겼다. 등록 현황 화면은 `완료` 텍스트를 유지한다.
- 가격·등록 필요 머리글은 중앙 정렬, 본문 금액만 우측 정렬이다. 금액 복사 버튼은 왼쪽 여백에 두어 숫자를 가리지 않는다.
- 상품 모달 높이를 `기본 설정` 탭 높이로 고정해, `이미지 설정`으로 바꾸거나 돌아와도 모달 크기가 변하지 않고 본문만 스크롤한다.
- 새 리스트 추가: 이름 입력 → 시작 방법(엑셀 불러오기 / 직접 입력하기) 2단계 모달. 이름이 비면 넘어가지 않고, 이전으로 돌아가도 이름이 유지되며, 만든 뒤 위저드가 완전히 닫힌 다음 선택한 방식으로 이어진다.
- anime.js가 있으면 세그먼트 탭 인디케이터가 선택 탭 위치·너비에 정확히 붙고, 없으면 인디케이터 없이 CSS 선택 배경이 그대로 유지된다. 두 경우 모두 콘솔 오류가 없다.
- 390px에서 위저드가 가로 넘침 없이 표시된다.

### 남은 제한

- `favicon.ico` 파일 자체는 저장소 루트에 사용자가 직접 올린다. 코드에서는 모든 페이지가 `/favicon.ico`를 참조하도록 연결만 해 두었다.
- anime.js와 Pretendard는 CDN에서 받으며 이 샌드박스에서는 접근이 차단되어 실제 로딩을 확인하지 못했다. 둘 다 실패해도 화면과 동작은 그대로 유지되도록 만들었고, 그 상태를 검수했다.
- 배포와 운영 Supabase 적용은 하지 않았다.
